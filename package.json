{
  "name": "pi-handoff",
  "version": "0.2.1",
  "description": "OMP-style session handoff for the pi coding agent. One /handoff command writes a structured handoff document and starts a fresh session that continues the work. Install with pi install.",
  "keywords": ["pi-package", "pi-extension", "handoff", "sessions", "omp"],
  "license": "MIT",
  "scripts": {
    "test": "bun test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*"
  },
  "pi": {
    "extensions": ["./handoff.ts"]
  }
}
